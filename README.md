# pulse-desk-app
Spring Boot backend that accepts user comments, sends them to the Hugging Face Inference API for AI analysis, and conditionally converts them into structured support tickets. It includes a bonus React-like frontend (served by Spring Boot itself) and is deployable via Docker.
